{
  "private": true,
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "8000"
    }
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.4"
  }
}
